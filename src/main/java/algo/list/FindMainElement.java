package algo.list;

import java.util.function.Function;

/**
 * 问题描述：
 *      已知一个整数序列A = (a_0, a_1, ..., a_n-1), 0 <= a_i < n
 *      若存在a_p1 = a_p2 = ... = a_pm = X, m > n/2
 *      则称X是序列A中的主元素
 * 案例：
 *      A = (0, 5, 5, 3, 5, 7, 5, 5) 则5为主元素
 * see also:
 *      2013「408统考」； 王道数据结构/线性表/综合应用题
 */
public class FindMainElement implements Function<int[], Integer> {
    @Override
    public Integer apply(int[] arr) {

        return null;
    }
}
